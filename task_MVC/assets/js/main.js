import '../css/main.css';

/*
console.log("Аякс запрос на добавление альпиниста в группу");
document.forms.signup.addEventListener('submit', function (e) {
    e.preventDefault();
    fetch('/signup', {
        method: 'post',
        body: new FormData(this)
    }).then(response => response.text())
        .then(text=>{
        if (text === 'success') console.log("Вы добавлены в группу!");
        else console.log("Вы не были добавлены в группу")
    })
})
/!*
if ($this->climberDB->addToExpedition($climber['id'], $expedition_id)) {
    echo 'success';
} else {
    echo 'error';
}*!/

document.forms.signup.addEventListener('submit', function (e) {
    e.preventDefault();
    fetch('/signup', {
        method: 'post',
        body: new FormData(this)
    })
        .then(response => response.text())
        .then(text => {
            if (text === 'success') console.log("Вы добавлены в группу");
            else console.log("Вы не были добавлены в группу");
        })
});*/
